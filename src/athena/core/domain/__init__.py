"""Domain models for the Athena platform.

All types in this package are frozen dataclasses or NewType wrappers.
Once constructed, they are never mutated. This makes them safe to share
across engines without defensive copying.
"""
